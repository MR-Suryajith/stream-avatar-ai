{
  "name": "obs-overlay-netlify",
  "version": "1.0.0",
  "description": "YouTube OBS Overlay hosted on Netlify",
  "scripts": {
    "dev": "netlify dev",
    "deploy": "netlify deploy --prod"
  },
  "dependencies": {
    "@netlify/blobs": "^7.3.0",
    "dotenv": "^17.3.1"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  }
}
